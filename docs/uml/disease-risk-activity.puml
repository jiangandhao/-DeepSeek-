@startuml
skinparam defaultFontName "SimHei"
skinparam defaultFontSize 14

title 疾病风险预警 - 活动图

start
:系统定期执行风险评估;

:系统查询用户健康数据;
:系统查询血糖数据;
:系统查询体检报告;
:系统查询用户基本信息;

:系统运行风险评估规则引擎;

:系统计算各疾病风险评分;

if (风险等级？) then (低风险)
  :标记为低风险;
  :更新用户风险档案;
elseif (中风险) then
  :标记为中风险;
  :生成风险提示;
  :系统发送预警通知;
  :显示风险提示和建议;
else (高风险)
  :标记为高风险;
  :生成紧急预警;
  :系统发送高风险预警;
  :显示紧急预警信息;
  :提供干预建议;
  :建议用户就医;
endif

:用户查看风险评估报告;
:系统显示风险详情;

if (需要干预？) then (是)
  :系统生成个性化干预方案;
  :显示干预建议;
  :用户确认干预计划;
  :系统记录干预计划;
else (否)
  :用户关闭风险报告;
endif

stop
@enduml
